# Ruby has singleton methods that function similarly to 'class' or 'static' methods in languages like Java, but the
# implementation is different. Ruby's 'class methods' are really just instance methods defined in the singleton class.
# Ruby doesn't have static methods like Java.

